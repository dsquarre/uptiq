run_name: base_run
dataset_path: data/processed/squad_v2_1200.jsonl
results_dir: results
seed: 42

architectures:
  - simple_rag
  - agentic_rag

retrieval:
  top_k: 3

agentic:
  max_steps: 3

judge:
  mode: local  # local | hybrid | api
  sample_size: null  # null = full dataset

tracking:
  record_latency: true
  record_cost: false
