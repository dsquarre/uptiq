run_name: low_budget_run
dataset_path: data/processed/squad_v2_1200.jsonl
results_dir: results
seed: 42

architectures:
  - simple_rag
  - agentic_rag

retrieval:
  top_k: 3

agentic:
  max_steps: 2

judge:
  mode: hybrid
  sample_size: 250

tracking:
  record_latency: true
  record_cost: false
